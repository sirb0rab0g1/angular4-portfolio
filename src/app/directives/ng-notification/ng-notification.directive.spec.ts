import { NgNotificationDirective } from './ng-notification.directive';

describe('NgNotificationDirective', () => {
  it('should create an instance', () => {
    const directive = new NgNotificationDirective();
    expect(directive).toBeTruthy();
  });
});
